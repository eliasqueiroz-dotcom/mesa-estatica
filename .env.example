# Copie para .env (gitignored) e preencha com os valores do seu projeto Supabase
# (Project Settings → API). Sem essas vars o app roda 100% local, sem multiplayer.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
